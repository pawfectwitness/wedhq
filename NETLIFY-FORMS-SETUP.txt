WedHQ V43
Both forms retain data-netlify=true and hidden form-name fields.
V43 removes the optional honeypot and uses explicit URLSearchParams construction for broader mobile Safari compatibility.
After deploy, test BOTH forms on iPhone/mobile and desktop. A successful production test requires:
1) inline success,
2) submission visible in Netlify Forms,
3) notification email received.
